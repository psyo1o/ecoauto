# 그룹웨어 연동 — 거래처·시설 매칭 스펙

> **수신 정본:** `\\192.168.10.163\docker\approval_mvp\docs\ECO_INPUT_MATCHING.md`  
> 작성: 2026-07-01 / 최종 갱신: 2026-09-12 (수신 스펙 정렬 · standard_unit · site_no=H6)  
> 발신: 측정인 자동화 프로그램 (`eco_input` / `groupware_client.py`)  
> 수신: `approval_mvp` — `match_company` / `ReportDataIn` / `upsert_self_measurement`

매칭 우선순위·금지사항·재전송 규칙은 **수신 정본만** 따른다. 이 문서는 발신 셀 매핑·엑셀 출처 보조.

---

## 1. 배경

측정인 자동입력 도구가 탭4 **입력완료** 후 그룹웨어 API로 측정 데이터·PDF를 전송한다.  
거래처 관리카드의 **시설**과 성적서를 연결하려면, 성적서 엑셀에만 있는 키를 사용해야 한다.

**권장:** 항상 `site_no` = 마스터 H (성적서 입력!H6).  
없으면 `company_name` = 마스터 G **전체**(짧은 이름만 보내지 말 것).

---

## 2. 적용 범위

| 매체 | 그룹웨어 연동 | 시설 매칭 |
|------|---------------|-----------|
| **대기 (air)** | ✅ 적용 | ✅ `facility_name` → SEMS 폴백 |
| **수질 (water)** | ❌ **미적용** | ❌ 해당 없음 |

수질 자동입력(`_main_water`)에 넣었던 그룹웨어 호출은 **주석 처리**되어 있다.

---

## 3. 성적서 엑셀 — 「입력」 시트 셀 정의 (대기 전용)

그룹웨어 **거래처 = 사업장(G열)**.  
`company_name`에 공장 구분된 사업장명을 보내야 한다 (C열 의뢰기관명만 보내면 위매스 1·2공장처럼 오매칭 가능).

| 성적서 셀 | 한글명 | API 필드명 | 그룹웨어 대응 | 용도 |
|-----------|--------|------------|---------------|------|
| **H6** | 사업장관리번호 | `site_no` | **사업장관리번호(H열)** | **있으면 거래처 매칭 최우선** |
| **H7** (라벨 G7 업체명) | 업체명/사업장명 | `company_name` | **사업장(G열)** | 표시·폴백 |
| **E4** (라벨 D4 측정인 시설명) | 측정인 시설명 | `facility_name` | **측정시설명(K열)** | **시설 1차** |
| **E2** | SEMS 일련번호 | `sems_stack` (+ `sems_serial`) | 시설 `sems_stack` | 시설 폴백 |

※ **E1(굴뚝번호)은 필드로 전송하지 않음** (폴백 힌트는 E2/`measurements[].stack`/파일명 `NO.n`).  
※ H7이 비면 대기측정기록부 **D3** 상호(사업장명)을 보조로 사용.  
※ **의뢰기관명만** `company_name`으로 보내지 말 것.  
※ `biz_no`는 있어도 되고 **매칭에 의존하지 말 것**.

### 정규화 규칙

| 필드 | 규칙 |
|------|------|
| `company_name` | 원문(H7) 그대로 + `company_name_norm` (법인표기·공백·특수문자 제거, 공장구분은 유지) |
| `facility_name` (E4) | trim 유지 (수신측이 정규화·부분일치) |
| `sems_stack` (E2) | 앞뒤·내부 공백 전부 제거 |

---

## 4. 수신측 매칭 (요약 — 정본은 ECO_INPUT_MATCHING.md)

### 거래처
1. `site_no` → 끝  
2. `company_name` 정확  
3. `biz_no` 단독(후보 1개만) / +이름  
4. `company_name` 부분일치(짧은 이름 주의)

### 시설
1. `facility_name` (정확→정규화→부분)  
2. 폴백: `sems_stack` / `SEMS일련번호*` / `measurements[].stack` / `source_excel`의 `NO.n`

시설명 미전송으로 시설이 안 붙으면 **status=warn**.

---

## 5. measurements[] (대기)

| 필드 | 출처 |
|------|------|
| `item_name` | 입력(분석값) B열 |
| `concentration` | 입력(분석값) 농도 열 |
| `unit` | 대기측정기록부 **I열** (농도 단위) |
| `standard` | 입력(분석값) **H열** 기준치 |
| `standard_unit` | 대기측정기록부 **F열** (허용기준 단위) |
| `flow_rate` | J14/N14 (`공기비적용`) — **Sm³/min**. 비산은 비어 있음(정상) |
| `stack` | 배출구 열 (시설 폴백 보조) |

---

## 6. 엔드포인트

| 방식 | 경로 | 비고 |
|------|------|------|
| A | `POST /api/reports/sync` | Form `data`=JSON + PDF |
| B | `POST /api/external/report_data` | JSON |
| B | `POST /api/external/report_pdf` | Form: `sample_no`, `company_name`, **`site_no`**, **`facility_name`**, … + file |

`site_no`는 JSON과 PDF Form **둘 다**.

---

## 7. 발신 체크리스트

1. [ ] `site_no` = H6 (마스터 H)  
2. [ ] `company_name` = H7 (마스터 G 전체)  
3. [ ] `facility_name` = E4 (마스터 K)  
4. [ ] `sems_stack` = E2 (폴백)  
5. [ ] report_data / sync / report_pdf 모두 `site_no`  
6. [ ] `standard` + `standard_unit` (농도 `unit`과 별도)  
7. [ ] `flow_rate` (Sm³/min)

---

## 8. 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-07-01 | 초안 — E2/E4 시설 매칭 |
| 2026-08-25 | `site_no` 전송 |
| 2026-09-12 | 수신 정본 `ECO_INPUT_MATCHING.md` 정렬: H6=`site_no`, 시설=`facility_name`→`sems_stack`, `standard`/`standard_unit`, unit=I·standard_unit=F |
