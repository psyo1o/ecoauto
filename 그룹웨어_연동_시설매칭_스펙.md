# 그룹웨어 연동 — 거래처·시설 매칭 스펙

> 작성: 2026-07-01 / 최종 갱신: 2026-08-05 (V3.5)  
> 발신: 측정인 자동화 프로그램 (`eco_input` / `groupware_client.py`)  
> 수신: 결재·그룹웨어(FastAPI) 개발 측 — `external_api.py` / 관리카드 시설 매칭 로직 반영용

---

## 1. 배경

측정인 자동입력 도구가 탭4 **입력완료** 후 그룹웨어 API로 측정 데이터·PDF를 전송한다.  
거래처 관리카드의 **시설**과 성적서를 연결하려면, 성적서 엑셀에만 있는 키를 사용해야 한다.

**중요:** 성적서 엑셀에는 **사업자등록번호가 없다.**  
진위확인·자가측정 매칭은 **업소명 + 시설키 + 시료번호 + 채취시간** 조합을 기본으로 한다.

---

## 2. 적용 범위

| 매체 | 그룹웨어 연동 | 시설 매칭 E2/E4 |
|------|---------------|-------------------|
| **대기 (air)** | ✅ 적용 | ✅ 적용 |
| **수질 (water)** | ❌ **미적용** | ❌ 해당 없음 |

수질 자동입력(`_main_water`)에 넣었던 그룹웨어 호출은 **주석 처리**되어 있다.

---

## 3. 성적서 엑셀 — 「입력」 시트 셀 정의 (대기 전용)

| 셀 | 한글명 | API 필드명 | 용도 |
|----|--------|------------|------|
| **H7** | 업소명 | `company_name` | 거래처(관리카드) 1차 식별 |
| **E2** | SEMS 일련번호 | `sems_serial` | 시설 1차 매칭 |
| **E4** | (시설 보조 키) | `facility_alt` | 시설 2차 매칭 |

※ **E1(굴뚝번호)은 사용하지 않음.**

자동화 프로그램은 위 3개 값을 **정규화한 뒤** payload에 실어 보낸다.

### 정규화 규칙 (자동화·그룹웨어 DB 동일 적용 권장)

| 필드 | 규칙 |
|------|------|
| `company_name` | 원문(H7) 그대로 전송 + `company_name_norm` (법인표기·공백·특수문자 제거) |
| `sems_serial` (E2) | 앞뒤·**내부 공백 전부 제거** |
| `facility_alt` (E4) | 동일 — **시설 보조키는 공백 제거 후 매칭** |

예: E4 `문의 휴 게소` → `facility_alt`: `문의휴게소`

---

## 4. 그룹웨어 측 권장 매칭 알고리즘

### 4-1. 거래처(관리카드) 찾기

```
1) `company_name_norm` (또는 H7 원문 정규화) 으로 관리카드 검색
2) 실패 시 → 수동 매칭 큐 / 오류 로그
```

※ `biz_no` 는 자동화 payload 에 **보내지 않음** (엑셀에 없음).  
포털 진위확인에서 사업자번호를 쓰는 경우, **관리카드 DB에만** 존재하는 값으로 별도 입력·검증.

### 4-2. 시설(관리카드 내 시설 행) 찾기

동일 거래처(`company_id`) 안에서 **아래 순서대로** 시설 1건을 매칭:

| 순위 | payload 필드 | 성적서 셀 | 관리카드 DB 매칭 대상 (예시) |
|------|--------------|-----------|------------------------------|
| 1 | `sems_serial` | 입력!E2 | `facility.sems_serial` |
| 2 | `facility_alt` | 입력!E4 | `facility.alt_key` (**공백 제거 후** 비교) |

```
for key in [sems_serial, facility_alt]:
    if key is blank: continue
    facility = find_facility(company_id, key, key_type)
    if facility: break
if not facility:
    → 미매칭 상태로 저장 + 관리자 알림 (또는 기본 시설)
```

### 4-3. 자가측정 행 저장

매칭된 `company_id` + `facility_id` 에 연결:

- `sample_no`, `collected_at`(**필수**, `HH:MM~HH:MM`), `collect_start`/`collect_end`, `measure_date`
- `measurements[]` (항목별 농도·단위·유량)
- PDF 파일 (sync API)

자동화는 **빈 `collected_at`으로 재전송하지 않음** (DB 덮어쓰기 방지).  
그룹웨어도 빈 `collected_at`으로 기존 값을 지우지 않는 것이 안전하다.

**진위확인 키:** `sample_no` + `collected_at` (+ 선택: `company_name` / 사업자번호는 포털·관리카드 측)

---

## 5. API JSON payload (변경 후, 대기 전용)

`POST /api/external/report_data`  
`POST /api/reports/sync` 의 `data` 필드

```json
{
  "media": "air",
  "sample_no": "A2604181-03",
  "company_name": "○○주식회사",
  "company_name_norm": "○○",
  "sems_serial": "SEMS-12345",
  "facility_alt": "문의휴게소",
  "facility_match_order": ["sems_serial", "facility_alt"],
  "facility_name": "문의휴게소",
  "collected_at": "13:10~15:30",
  "collect_start": "13:10",
  "collect_end": "15:30",
  "measure_date": "2026-04-18",
  "measurements": [
    {
      "stack": "1",
      "item_name": "먼지",
      "concentration": "12.3",
      "unit": "mg/S㎥",
      "flow_rate": "12345"
    }
  ],
  "source_excel": "\\\\NAS\\...\\성적서.xlsm"
}
```

`collected_at` / 날짜 출처:

| 구분 | 채취시간 셀 | 비고 |
|------|-------------|------|
| 일반 | 대기측정기록부 **F23~I23** | `parse_measuring_record` / meta 재읽음 |
| 비산먼지 | 대기측정기록부 **F17~I17** | 파일명에 `비산먼지` |

`unit` / `flow_rate` 규칙 (대기 `groupware_client.read_air_measurements`):

| 필드 | 출처 |
|------|------|
| `item_name` | 입력(분석값) B열 |
| `concentration` | 입력(분석값) 농도 열 |
| `unit` | 대기측정기록부 **F열** (B열 항목명 매칭). 비산은 상단행(예: B19/F19)도 읽음 |
| `flow_rate` | 아래 표. **비산은 비어 있음(정상)** |

| 입력(분석값) `공기비적용` | 유량 출처 | 셀 |
|--------------------------|-----------|-----|
| 「적용」포함 · 「미적용」아님 (`적용`/`적용1`/`적용2`) | 보정후 | 대기측정기록부 **N14** |
| 그 외 (미적용 등) | 보정전 | 대기측정기록부 **J14** |

농도는 기존과 동일하게 `측정농도`/`농도` 열만 사용.

### 제거·비사용 필드

| 필드 | 상태 |
|------|------|
| `biz_no` | **전송 안 함** (엑셀에 없음) |
| `stack_no` / E1 | **전송 안 함** (굴뚝번호 미사용) |

### 신규·필수 권장 필드

| 필드 | 타입 | 설명 |
|------|------|------|
| `sems_serial` | string | 입력!E2 |
| `facility_alt` | string | 입력!E4 |
| `facility_match_order` | string[] | `["sems_serial", "facility_alt"]` |
| `company_name_norm` | string | 매칭용 정규화 업소명 |
| `facility_name` | string | UI 표시용 — **E4(시설명) 우선**. E2가 숫자 코드만이면 비움 (굴뚝번호로 덮지 않음) |
| `collected_at` | string | **필수** `HH:MM~HH:MM` |
| `collect_start` / `collect_end` | string | 채취 시작·끝 |

---

## 6. PDF multipart (`POST /api/reports/sync`)

Form 필드 예:

| name | 값 |
|------|-----|
| `data` | 위 JSON 문자열 |
| `file` | PDF 바이너리 — 아래 표 |

| 구분 | 병합 PDF |
|------|----------|
| 일반 | 대기측정기록부 + **먼지시료채취기록지**(있으면) |
| 비산먼지 | 대기측정기록부 + **교정** |
| 부가 시트 없음 | 대기측정기록부만 |

자동화 쪽: `make_tab4_pdfs()` 의 `pdf_groupware` (`{시료번호}__그룹웨어.pdf`).  
최종 병합본(`p1`)·측정인 업2(`pdf_record`)와는 별개.

`POST /api/external/report_pdf` Form 에도 `sems_serial`, `facility_alt` 권장 (`biz_no`, `stack_no` 제거).

---

## 6. 그룹웨어 DB·UI 반영 체크리스트

- [ ] `Facility` 모델에 `sems_serial`, `alt_key`(E4 대응) 컬럼 또는 매핑 테이블
- [ ] `external_api.py` 수신 시 `resolve_facility(company, payload)` — E2 → E4 순서
- [ ] 관리카드 시설 등록 UI — E2/E4 와 동일 키 입력 가능하게
- [ ] `/portal/report-verify` — 사업자번호 **필수**면 관리카드 DB 값만 사용 (엑셀·자동화와 분리)
- [ ] 자가측정 대장 — 매칭된 시설명 표시
- [ ] API 스키마: `biz_no`, `stack_no` optional / deprecated

---

## 7. 자동화 프로그램 측 구현 위치

| 파일 | 함수 |
|------|------|
| `3.py/groupware_client.py` | `read_input_sheet_keys()` — H7, E2, E4 읽기 |
| | `facility_match_keys()` — payload 조립 |
| | `build_payload_air()` |

---

## 8. 결재시스템 Cursor에 넘길 프롬프트 (복사용)

```
@docs/PORTAL.md @external_api.py

측정인 자동화 프로그램이 POST /api/reports/sync 로 보내는 JSON에
시설 매칭 필드가 추가되었다. 성적서 엑셀에는 사업자번호가 없다.

반영 요청:
1. payload: sems_serial(E2), facility_alt(E4), company_name(H7)
2. biz_no, stack_no(E1) 는 optional — 자동화는 안 보냄
3. 시설 매칭 순서: sems_serial → facility_alt
4. 관리카드 Facility 모델·external_api 수신 로직·report-verify 정책 정리

상세 스펙: (자동화 레포) 그룹웨어_연동_시설매칭_스펙.md
```

---

## 9. 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-07-01 | 초안 — E1/E2/E4 시설 매칭, biz_no 제거, H7 업소명 확정 |
| 2026-07-03 | 시설 보조키 E3→E4, **E1(굴뚝번호) 제거** — 매칭 E2→E4만 |
| 2026-07-29 | GUI 「그룹웨어 연동」체크박스 제거 — `config.ini` `ENABLED=1` 기본 전송 |
|            | 전송 실패 자동 재시도 (3회, 5초 간격) / 422 거래처 미매칭은 즉시 포기 |
|            | `GroupwareRunLog` — 시료별 결과 추적, `failed_samples()`, `summary_text()` |
|            | 완료 후 실패 목록 요약 출력 / GUI 팝업 재전송 / 콘솔 재전송 프롬프트 |
|            | PDF sync `timeout` 중복 버그 수정 (`_request_with_retry` kwargs pop) |
| 2026-07-30 | `measurements[].flow_rate` 추가 — 공기비적용(미적용→J14, 적용1/2→N14) |
|            | 첨부 PDF = 대기측정기록부 + 먼지시료채취기록지(있으면) 병합 (`pdf_groupware`) |
| 2026-07-31 | V3.5 — `collected_at`/`measure_date` 필수, meta 없어도 기록부 재읽음, 빈값 재전송 차단 |
|            | `collect_start`/`collect_end`, `facility_name`=E4 우선, API `warnings`/`verify_key` 표시 |
| 2026-08-04 | 비산 PDF = 기록부+**교정**, 비산 단위 상단 B/F 읽기, 공기비「적용」포함→N14 |
| 2026-08-05 | 스펙·readme·파일별_설명 문서 동기화 |
