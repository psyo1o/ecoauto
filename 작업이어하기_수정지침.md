# 작업 이어서 하기 · 수정 지침서

> 작성일: 2026-08-21 (갱신: 2026-09-12 — 그룹웨어 수신 정본 `ECO_INPUT_MATCHING.md`)  
> 목적: 다른 PC/채팅에서도 이 문서를 보고 **같은 맥락으로 수정**하고, 최근 변경을 되돌리거나 중복 구현하지 않기  
> 관련 채팅: [Cloud sync modification issue](eae5fa87-b03c-406d-8ca5-ad33f70b65ad)  
> GitHub: `https://github.com/psyo1o/ecoauto.git` (작업 브랜치 예: `cursor/v35-groupware-docs`)

---

## 0. 다른 곳에서 이어서 할 때 (체크리스트)

1. NAS 경로 열기: `\\192.168.10.163\측정팀\10.검토\1.검토 및 입력 프로그램`
2. 이 문서 + `버전노트.txt` 읽기
3. **그룹웨어 매칭·API 필드**를 건드리면 먼저 수신 정본 확인:  
   `\\192.168.10.163\docker\approval_mvp\docs\ECO_INPUT_MATCHING.md`  
   (로컬 보조: `그룹웨어_연동_시설매칭_스펙.md`)
4. `git status` / `git log -5` 로 **커밋된 것 vs 워킹트리만 있는 것** 구분
5. 수정 전 **대상 도구** 확인 (아래 §1 런처 번호)
6. `__pycache__`, `4.log\` 로그는 커밋하지 말 것
7. 커밋/푸시는 **사용자가 요청할 때만**

### 자주 건드리는 파일

| 영역 | 파일 |
|------|------|
| 측정인 실행 로직 | `3.py/eco_input.py` |
| 측정인 GUI | `3.py/eco_input_gui.py` |
| 탭4 매크로 읽기(대기) | `3.py/tab4_utils.py` |
| 탭4 수질 | `3.py/water_input_utils.py` |
| 엑셀 COM 공용 | `3.py/excel_com_utils.py`, `3.py/pdf_utils.py` |
| 그룹웨어 API/재전송 | `3.py/groupware_client.py` |
| 그룹웨어 GUI(8번) | `3.py/groupware_resend_gui.py` |
| 경로·토큰 | `config.ini`, `3.py/config.py` |
| 통합 런처 | `2.검토 및 입력프로그램.pyw` |
| 필수 패키지 목록 | `0.처음사용시/requirements.txt` (+ `req_utils.py`) |
| 첫 설치(통합) | `0.처음사용시/드라이버+파이썬패키지+보안경고 통합설치.py` — **개별 py 호출만** |

---

## 1. 프로그램 구조 (건드리면 안 되는 것 / 안전한 것)

### 런처 버튼 ↔ 스크립트

- `2.검토 및 입력프로그램.pyw` → `3.py\` 아래 스크립트 기동
- **8번** `그룹웨어 전송(재전송,직접)` → **`groupware_resend_gui.py`**  
  (파일이 없으면 8번이 안 켜짐. 복구 시 이 파일명 유지)

### 측정인: GUI vs 실행 로직

- **실행 본체**: `eco_input.py` 의 `_main_air` / `_main_water` (인자 이름·키워드 호출 유지)
- **GUI**: `eco_input_gui.py` 만 탭/체크박스 UI. 시작 시 `_main_*`에 키워드로 넘김
- GUI만 고칠 때 `_main_air`/`_main_water` 시그니처를 바꾸지 말 것

### 탭4가 쓰는 엑셀 2종류 (혼동 금지)

| 용도 | 파일 | 누가 읽나 |
|------|------|-----------|
| 사이트에 넣을 **날짜·항목 값** | `측정인 측정분석 입력 26.01.xlsm` (대기) / `26.05(수질).xlsm` | `read_tab4_from_macro_xlsm` / `read_water_tab4_from_macro_xlsm` |
| **PDF** 생성·업로드 | 시료별 **성적서** `.xlsm` | `make_tab4_pdfs` / `make_tab4_pdfs_water` |

매크로 시트명: `00. 측정분석결과 입력샘플` (A6에 시료번호)

### 수질 그룹웨어

- **수질은 그룹웨어 전송 미적용** (주석/스킵 유지). 대기만 전송.

---

## 2. 최근 수정 요약 (2026-08-13 ~ 09-11)

아래는 V3.6 이후, 채팅·로컬에서 다룬 핵심이다. `버전노트.txt`에도 동일 계열을 기록한다.

### 2-1. 측정인 GUI (V3.6 + 보완)

- 대기/수질 **Notebook** 분리. 시작은 **현재 열린 탭만** 실행
- 대기: 측정(탭1/2/백데이터) vs 분석(탭4). 탭 진입 시 해당 체크 전체 ON
- 수질: 탭2+탭4 동시 선택 유지
- `_sync_media_from_tab()`: 시작/드롭 시 `media_var`와 실제 탭 불일치 방지
- DnD 실패 시 `LogPanel.widget` 가정하지 말 것 → `print` / 실제 로그 위젯 사용

### 2-2. 그룹웨어 8번 GUI · 재전송

**파일**: `groupware_resend_gui.py` (탭1=로그 재전송, 탭2=성적서 직접 전송)

| 기능 | 동작 |
|------|------|
| 직접/재전송 | 데이터+PDF (`_sync_excel_with_pdf` → `make_tab4_pdfs(..., copy_to_nas=False)` → `_try_groupware_tab4_sync`) |
| NAS 복사 | 그룹웨어 경로에서는 **`0 5.최종완료` / `0.PDF`에 넣지 않음** (`copy_to_nas=False`) |
| PDF 없음 조회 | 체크 **「PDF 없음 포함」** (기본 ON). 로그 `PDF전송`이 N/A·빈값인 건도 대상 |
| 완료 후 재조회 | `재전송상태=완료`이면 **예전 로그의 데이터전송=FAIL은 무시**. PDF 없음/FAIL만 다시 표시 |
| 성공 시 로그 갱신 | `재전송상태=완료` + 가능하면 `PDF전송=OK`, `데이터전송=OK` |
| openpyxl 경고 | Data Validation / Conditional Formatting UserWarning 숨김 |

**재전송이 “빠진 쪽만” 올리는가?**

- **아니오.** 조회 사유는 PDF 없음/데이터 FAIL 등으로 나뉘지만, 재전송 시에는 **데이터+PDF를 다시 둘 다** 보낸다 (`reports/sync` 우선).
- “PDF만 / 데이터만” 분기는 **아직 없음**. 필요하면 `groupware_client.sync_to_groupware` / `resend_pending_batch`를 의도적으로 바꿔야 함.

**전송 로그 위치**

- `\\192.168.10.163\측정팀\10.검토\6.그룹웨어전송\{YYYY}년\{M}월\그룹웨어전송_YYYYMMDD_HHMMSS.xlsx`

### 2-3. 엑셀 시트 다중선택 해제 (2026-08-20)

**문제**: Ctrl로 시트 여러 장 선택된 채 저장 → COM에서 `Range` 쓰기 / `ExportAsFixedFormat`이 선택 시트 전부에 적용될 수 있음 (특히 탭4 매크로 A6 입력·PDF).

**해결**: `excel_com_utils.ungroup_excel_sheets(wb, prefer_sheet=None)`  
열자마자 한 시트만 `Select(True)`. 실제로 그룹이었을 때만 로그:  
`▶ 엑셀 시트 다중선택 해제 → '시트명'`

**적용 위치**

- `tab4_utils` / `water_input_utils` (매크로 읽기)
- `pdf_utils.PDFExporter` (시트별 PDF)
- `eco_input.export_pdf_from_excel`, `_read_excel_cell_com`
- `backdata_utils` (수분/THC COM)

**탭1·2 사이트 입력값**: openpyxl이 **시트 이름**으로 읽음 → 다중선택과 무관.  
**영향**: 정상(단일 선택) 파일은 동작 동일. 그룹일 때만 안전장치.

---

### 2-4. 그룹웨어 거래처 = 사업장 (2026-08-24 / 정본 정렬 2026-09-12)

> 수신 정본: `\\192.168.10.163\docker\approval_mvp\docs\ECO_INPUT_MATCHING.md`

| payload | 성적서 | 그룹웨어 |
|---------|--------|----------|
| `site_no` | 입력!H6 | 사업장관리번호(H열) — **최우선** |
| `company_name` | 입력!H7 업체명(사업장) | 사업장(G열) 전체 |
| `facility_name` | 입력!E4 측정인 시설명 | 측정시설명(K열) — 시설 1차 |
| `sems_stack` | 입력!E2 | 시설 폴백 |
| `biz_no` | 보통 없음(빈 문자열) | 보조만 |

- C열 의뢰기관명만 `company_name`으로 보내지 말 것 (위매스 1·2공장 오매칭)
- 수정 위치: `groupware_client.read_input_sheet_keys` / `build_payload_air` (eco_input·8번 공통)

---

### 2-5. 설치·패키지·런처 (2026-09-11)

| 항목 | 내용 |
|------|------|
| 패키지 목록 | `0.처음사용시/requirements.txt` 단일 소스 |
| 로더 | `req_utils.py` (`load_pip_packages`, `IMPORT_ALIASES`) |
| 통합설치 | 로직 복제 없음 → `보안경고시 실행.py` / 크롬 / `파이썬 패키지.py` 순차 호출 (`--no-pause`) |
| 보안 | `보안경고시 실행.py` 가 winreg로 직접 적용 (구 `.REG` 삭제) |
| 런처 | 시작 시 미설치 패키지 안내 → pip → 재시작 (ChromeDriver/보안설정은 미포함) |

---

## 3. 수정할 때 지킬 지침 (에이전트·사람 공통)

### 3-1. 범위

- 요청한 도구/파일만 수정. 무관한 리팩터·문서 대량 수정 금지 (이 지침서·버전노트 갱신은 예외)
- `4.log`, `__pycache__`, 임시 PDF는 커밋 대상 아님

### 3-2. 측정인 / 탭4

- `_main_air` / `_main_water` 인자·키워드 깨지 말 것
- 탭4 **값** = 매크로 xlsm, **PDF** = 성적서 — 경로/역할을 섞지 말 것
- `make_tab4_pdfs(..., copy_to_nas=)`  
  - 측정인 탭4 정상 흐름: 기본 `True` (NAS 최종본)  
  - 그룹웨어 재전송/직접: 반드시 `False`
- COM으로 워크북 새로 열면 **가능하면** `ungroup_excel_sheets` 호출 유지

### 3-3. 그룹웨어

- **수신 정본(우선):** `\\192.168.10.163\docker\approval_mvp\docs\ECO_INPUT_MATCHING.md`
- 발신 셀 매핑 보조: `그룹웨어_연동_시설매칭_스펙.md`
- 핵심: `site_no`(H6), `company_name`(H7=G전체), `facility_name`(E4), `sems_stack`(E2 폴백)
- `standard` + `standard_unit`(F) / 농도 `unit`(I) 분리
- 수질 그룹웨어 미적용 유지
- 재전송 조회 로직 변경 시:
  - `_classify_sample_resend_need`
  - `collect_pending_resends`
  - `mark_resend_status_in_log`
  - GUI `groupware_resend_gui._on_scan`
  를 **같이** 맞출 것
- `재전송상태=스킵`만 무조건 제외. `완료`여도 PDF 없음/FAIL은 다시 나올 수 있게 유지 (의도된 동작)
- 성공 마킹만 하고 PDF/데이터 열을 옛 FAIL로 두면 재조회 버그가 다시 난다 → `pdf_status`/`data_status` 갱신 유지

### 3-4. GUI / DnD

- `tkinterdnd2` 없을 때 `tk.Tk()` 폴백
- 로그 위젯 속성명 추측하지 말 것 (`print` 또는 해당 클래스의 실제 위젯)

### 3-5. 설치 / 패키지 / 런처

- **pip 패키지 추가·삭제**: `0.처음사용시/requirements.txt` 만 수정  
  - `파이썬 패키지.py` · 런처 · 통합설치에 목록을 다시 하드코딩하지 말 것
- **import 이름 다른 패키지** (`pywin32`→`win32com`, `pillow`→`PIL` 등): `req_utils.py` 의 `IMPORT_ALIASES`
- **보안 설정**: `보안경고시 실행.py` (`NAS_IP`, winreg). 구 `.REG` 파일은 삭제됨
- **통합설치.py**: 오케스트레이터만 (`보안` → `크롬` → `패키지`, `--no-pause`). 설치 로직을 여기 복제하지 말 것
- **런처 패키지 자동설치**: `2.검토 및 입력프로그램.pyw` 시작 시 txt 기준으로 검사·설치·재시작. ChromeDriver/보안설정은 런처에서 안 함

### 3-6. 테스트 최소 확인

| 변경 | 확인 |
|------|------|
| 탭4/매크로 | 시료 1건 탭4 → 그리드 값·PDF 정상 |
| 그룹웨어 재전송 | 기간 조회 → PDF 없음 건 표시 → 재전송 성공 → **재조회 0건**(또는 해당 시료 제외) |
| `copy_to_nas=False` | 재전송 후 `0 5.최종완료`/`0.PDF`에 **새 파일 안 생김** |
| 시트 그룹 | (가능하면) 시트 2장 선택 저장본 → 로그에 해제 메시지, 값/PDF 정상 |
| 패키지 목록 | `requirements.txt` 수정 후 `파이썬 패키지.py` 또는 런처 기동으로 반영 확인 |
| 문법 | `python -m py_compile` 해당 파일 |

---

## 4. 의도적으로 아직 안 한 것 / 주의

1. **재전송 시 PDF만/데이터만 분리 업로드** — 미구현. 지금은 항상 데이터+PDF.
2. **수질 그룹웨어** — 미적용 유지.
3. 클라우드 브랜치와 로컬 워킹트리가 어긋날 수 있음 → 이어서 작업 전 `git status` 확인.  
   겹치는 파일은 “어느 쪽이 최신 의도인지” 확인 후 merge/stash.
4. openpyxl Data Validation 경고는 **숨김**만 함. 데이터 검증 기능 제거 아님.

---

## 5. 빠른 코드 앵커

```text
ungroup_excel_sheets     → 3.py/excel_com_utils.py
재전송 사유 분류         → groupware_client._classify_sample_resend_need
기간 조회                → groupware_client.collect_pending_resends
데이터+PDF 1건           → groupware_client._sync_excel_with_pdf
PDF NAS 복사 스위치      → eco_input.make_tab4_pdfs(..., copy_to_nas=)
탭4 매크로(대기)         → tab4_utils.read_tab4_from_macro_xlsm
GUI 매체 동기화          → eco_input_gui._sync_media_from_tab
8번 GUI                  → groupware_resend_gui.py
패키지 목록              → 0.처음사용시/requirements.txt
패키지 로더              → 0.처음사용시/req_utils.py
통합설치(호출만)         → 0.처음사용시/드라이버+파이썬패키지+보안경고 통합설치.py
런처 패키지 검사         → 2.검토 및 입력프로그램.pyw (ensure_dependencies)
```

---

## 6. 문서 유지 규칙

- **동작/버전 한 줄 기록** → `버전노트.txt` (기존 형식 유지)
- **이어서 작업·수정 주의** → 이 파일(`작업이어하기_수정지침.md`)에 추가
- 사용자 안내용 긴 설명 → `readme.md` / `파일별_설명.md`
- **그룹웨어 매칭·API 필드(수신 정본)** → `\\192.168.10.163\docker\approval_mvp\docs\ECO_INPUT_MATCHING.md`
- 발신 셀 매핑 보조 → `그룹웨어_연동_시설매칭_스펙.md` (수신 정본과 충돌 시 정본 우선)
- 필수 pip / 첫 설치 구조 → `readme.md` §2, `파일별_설명.md` §6

새 기능을 넣으면 이 문서 **§2에 날짜·요약 한 블록**을 追記하고, 필요 시 §3 지침을 한 줄 보강한다.
